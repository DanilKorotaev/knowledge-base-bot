# Обработка голосовых сообщений

**Статус**: ✅ Реализовано  
**Приоритет**: 🔴 Высокий  
**Категория**: Новые функции

## Описание

Реализация обработки голосовых сообщений с транскрибацией через Whisper API и последующей обработкой как текстовых сообщений.

## Цели

1. Реализовать скачивание голосовых файлов из Telegram
2. Реализовать транскрибацию через Whisper API
3. Реализовать сохранение транскрипции в БД
4. Реализовать обработку транскрипции как текстового сообщения
5. Реализовать команду `/transcribe` для отдельной расшифровки

## Задачи

- [x] Реализовать скачивание голосового файла из Telegram ✅
- [x] Реализовать отправку в Whisper API для транскрибации ✅
- [x] Реализовать сохранение транскрипции в БД ✅
- [x] Реализовать обработку транскрипции как текстового сообщения ✅
- [x] Реализовать красивое форматирование расшифровки (как в ChatGPT) ✅
- [x] Реализовать сохранение языка транскрипции ✅
- [x] Реализовать команду `/transcribe` для отдельной расшифровки ✅
- [ ] Реализовать обработку больших файлов (разбиение на части) ⚠️ Не требуется для голосовых сообщений Telegram (лимит ~20 МБ, Whisper API поддерживает до 25 МБ)
- [x] Реализовать поддержку форматов: OGG, MP3, WAV ✅ (Whisper API поддерживает автоматически)
- [x] Реализовать получение последнего голосового сообщения для команды `/transcribe` ✅
- [x] Реализовать обработку ошибок при скачивании/транскрибации ✅
- [x] Реализовать очистку временных файлов после обработки ✅

## Особенности

- ✅ Поддержка форматов: OGG, MP3, WAV (Whisper API поддерживает автоматически)
- ⚠️ Обработка больших файлов (разбиение на части) - не требуется для голосовых сообщений Telegram
- ✅ Красивое форматирование расшифровки (HTML/Markdown)
- ✅ Сохранение языка транскрипции в БД
- ✅ Автоматическая обработка транскрипции как текстового сообщения
- ✅ Команда `/transcribe` для повторной расшифровки последнего голосового сообщения
- ✅ Обработка ошибок на всех этапах
- ✅ Автоматическая очистка временных файлов

## Технические детали

### Пример реализации

```python
# В handlers/voice.py
from services.transcription_service import TranscriptionService
from services.openai_service import OpenAIService

@dp.message(F.voice)
async def voice_handler(message: Message, state: FSMContext):
    """Обработка голосовых сообщений"""
    # Скачать файл
    file = await bot.get_file(message.voice.file_id)
    audio_path = f"/tmp/{file.file_id}.ogg"
    await bot.download_file(file.file_path, audio_path)
    
    # Транскрибировать
    openai_service = OpenAIService()
    transcription_service = TranscriptionService(openai_service)
    result = await transcription_service.transcribe(audio_path)
    
    # Сохранить транскрипцию в БД
    session = await db.get_active_session(message.from_user.id)
    if session:
        attachment = await db.add_attachment(
            session_id=session["id"],
            file_type="voice",
            file_id=message.voice.file_id,
            file_name=f"{file.file_id}.ogg",
            file_size=message.voice.file_size
        )
        await db.add_transcription(
            attachment_id=attachment["id"],
            text=result["text"],
            language=result["language"]
        )
    
    # Отправить красивую расшифровку
    await message.answer(
        f"🎤 **Расшифровка:**\n\n{result['text']}",
        parse_mode="Markdown"
    )
    
    # Обработать как текстовое сообщение
    if session:
        await process_text_query(result['text'], message, state)
```

## Связанные файлы

- ✅ `handlers/voice.py` - обработчик голосовых сообщений (реализовано)
- ✅ `services/transcription_service.py` - сервис транскрибации (существовал, используется)
- ✅ `services/openai_service.py` - работа с OpenAI API (Whisper) (существовал, используется)
- ✅ `handlers/commands.py` - команда `/transcribe` (реализовано)
- ✅ `utils/file_helpers.py` - утилита для скачивания файлов из Telegram (добавлено)
- ✅ `database/base.py` - методы для работы с транскрипциями (добавлено)
- ✅ `database/sqlite_db.py` - реализация для SQLite (добавлено)
- ✅ `database/postgresql_db.py` - реализация для PostgreSQL (добавлено)

## Реализованные функции

### Обработка голосовых сообщений
- Автоматическое скачивание голосовых файлов из Telegram
- Транскрибация через Whisper API (OpenAI)
- Сохранение транскрипции и метаданных в БД
- Автоматическая обработка транскрибированного текста как обычного запроса
- Красивое форматирование расшифровки (HTML/Markdown)
- Отображение языка транскрипции
- Обработка ошибок на всех этапах
- Автоматическая очистка временных файлов

### Команда `/transcribe`
- Получение последнего голосового сообщения пользователя
- Показ существующей транскрипции (если уже была расшифрована)
- Повторная расшифровка при необходимости
- Сохранение транскрипции в БД для повторного использования

## Примечания

- Whisper API автоматически поддерживает множество форматов (OGG, MP3, WAV, M4A, FLAC и др.)
- Лимит Whisper API: 25 МБ на файл (голосовые сообщения Telegram обычно меньше)
- Обработка больших файлов (разбиение на части) не реализована, так как не требуется для голосовых сообщений Telegram

