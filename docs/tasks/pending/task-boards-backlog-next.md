# Boards / Overview — backlog

## Done recently
- Empty open-source seed; definitions via MCP/DB
- Generic `vault_frontmatter_agg` + optional `sidecar` (related note)
- `period=YYYY-MM|all` and `from`/`to` ISO dates on detail/refresh
- `definition.period_ui`: `none` | `month` | `range` (client toolbar)
- Readonly plaque only if `labels.readonly` is non-empty
- iOS: month wheels / range MultiDatePicker + Overview settings toggle
- MCP `kb-boards` + vault rule/skill

## Still open
1. Auto-invalidate after agent writes matching notes
2. In-app tool layer without MCP (optional; MCP covers chat agent on mini)
3. System board: query jobs + cancel
4. Remote boards / charts / vault_script provider
5. Multi-user bearer for MCP (later)
