# Telegram Bot Framework
aiogram==3.16.0
aiohttp-socks==0.10.1

# Database
psycopg2-binary==2.9.9
asyncpg==0.29.0
aiosqlite==0.20.0

# OpenAI API (for Whisper transcription)
# Обновлено до версии, совместимой с httpx
openai>=1.30.0,<2.0.0
httpx[socks,http2]>=0.27.0
PyJWT[crypto]>=2.8.0

# NextCloud WebDAV
requests==2.31.0
webdavclient3==3.14.6

# MCP (Model Context Protocol) — для MCP серверов, запускаемых cursor-agent
mcp>=1.0.0

# Utilities
python-dotenv==1.0.0
aiofiles==23.2.1
# Health linking (packages/health_linking) — Path 2 в боте + health-sync-api
python-frontmatter==1.1.0
PyYAML==6.0.2

# File watching (optional)
watchdog==3.0.0

# FastAPI (optional, for future API endpoints)
fastapi==0.115.0
uvicorn[standard]==0.32.0

